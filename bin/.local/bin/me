#!/bin/bash

if [ "$1" = "-h" ] || [ "$1" = "--help" ]; then
    echo "Usage: $(basename $0) [OPTION]"
    echo "Show the user's SSH prompt"
    echo "Options:"
    echo "  -h, --help    Show this help message and exit"
    exit 0
fi

echo "$(whoami)@$(cat /etc/hostname)"

